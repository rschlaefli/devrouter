# Prepare managed application dependencies before readiness

## Outcome and authority

Prevent dependency preparation from consuming Klicker's application readiness window while retaining unchanged-runtime reuse. Run preparation only after the existing helper has stopped a changed owned process group. Enforce a real90-second bound per application wait and preserve semantic response classifications.

The user requested the remaining general runtime fix and authorized continuation. This source-only package includes isolated worktrees, in-scope edits, focused tests, required reviews, local commits and updates to affected tasks. Terminal is verified, reviewed local source and consumer dispositions. Publishing, pushing, merging, installing a new version and live peer recovery require separate authority. Devrouter0.0.52 remains unchanged. No data deletion, recreation, bootstrap, broad cache clear or shared-router restart.

## Scope and ownership

Devrouter: branch rs/managed-startup-preparation at trees/rs/managed-startup-preparation, base f4c9cddb28221a489848206877c230ba73997089. Change the packaged process helper and its existing behavioral tests. Klicker: reuse trees/rs/fix-devcontainer-python-selection at e5483cf736e722def0868630d70f4a6fb2e96083; modify util/dev-runtime.sh, .devcontainer/post-start.sh and focused runtime tests. Existing UV_PYTHON correction remains part of the local consumer branch. The current origin/v3 delta is CI-only; no integration.

Main owns design, integration, consumer code, deadline and final evidence because these steps share runtime inputs. A bounded executor may implement the settled helper and its disjoint tests. Required full-path simplifier, slice and final reviews cover the combined source package. Native planner01a0728f-9686-7de1-8cda-f50562316357 approved preparation scope/ordering after same-child revision. No model changes or review waivers.

## Contract

Validate preparation options/selection before mutation. Preserve default fingerprint bytes for callers without preparation; opt-in preparation participates in identity. Under the existing helper lock, reuse unchanged owned runtimes without preparation. For a changed runtime, prove ownership, stop its exact group, run the existing foreign-process guard, prepare synchronously, then launch and publish owned state. Failed or cancelled preparation never launches dev. Preserve lock ownership through preparation and cancellation. Preparation commands must remain foreground processes in their original process group; daemonizing or detaching is unsupported. Terminate that group before releasing the lock, reap its direct child, and rely on container init to reap orphan zombies. This covers the pnpm/Turbo build command without claiming control of commands that deliberately escape supervision.

Klicker preparation derives selected roots from the existing resolver. Full roots come from the canonical dev:container script through a restricted grammar. Reject unsupported syntax, missing roots and malformed selectors before changes. Build only each selected root's dependency closure. Synchronize dependency installation before builds. Capability-only profiles do not prepare. The consumer requires a packaged helper supporting the hook and must fail before destructive/launch effects if unavailable.

Readiness measures elapsed time per application, caps each curl call and sleep to remaining time, never passes zero timeout, reports progress periodically, and distinguishes transport failure from HTTP classification. Preserve stale/unexpected response thresholds; transport failures reset stale evidence and never clear caches.

## Verification and risks

Synthetic Node24.16.0/Turbo2.10.11 proof passes dependency-only filtering, cached warm dev and failed-build prevention. Actual metadata graph comparison for manage, pwa, chat, live-quiz, chat,mcp, manage,pwa and full matches all nine executable dev build prerequisites with no app production builds. These are not live Klicker health proof.

Helper tests cover unchanged reuse skipping prep, changed group termination before prep, foreign refusal, failed/cancelled prep and lock retention. Consumer tests cover exact selection, capability no-op, preparation failure ordering, real hanging HTTP deadline, and unchanged semantic classifications. Run repository checks in isolated containers, inspect exact diffs and obtain required reviews before delivery. Stop all task-owned test containers. Browser and actual affected-workspace proof remain pending named live authority.

## Separate issues

Partial-stop completion remains a separate Devrouter package requiring exact retained container membership, ownership and full-service stopped proof. Chemistry's Turbopack failure remains unproven cache causation. Neither is silently folded into this preparation hook or considered fixed by this package.

## Progress

Planner retry succeeded; the earlier credit failure is no longer current. Klicker source is committed at 139c8d3fe90946740eecf6018c4948ec8e55e738. Existing profile/runtime checks pass, and the real hanging HTTP test exits in 90.424 seconds. Added tests cover dependency selection, preparation ordering and the deadline; no existing behavioral tests were removed. The actual seven profile/union graphs match all nine build prerequisites and exclude application production builds.

The helper and process regressions are implemented. Both executor and main-session Linux suites pass, including failed/cancelled preparation, nested TERM-ignoring children, lock contention, unchanged default fingerprints and opt-in reuse. Devrouter's unchanged TypeScript suite passes 881 tests with one existing skip. Documentation, knowledge, lint, Knip, type, build and isolated package checks pass, including the packaged preparation option. Simplification, lifecycle slice and integrated final reviews completed with no source changes requested. The source-only terminal is complete. Full Klicker monorepo check:all and actual consumer browser proof remain outside these focused source checks.

No peer runtime or dirty source changes were made. Six consumer tasks received the source status and activation boundary. Task-owned proof containers were stopped and verified absent. Partial-stop completion and Chemistry's Turbopack error remain separate pending issues. Release and install a helper with the new preparation option before activating the consumer; published 0.0.52 does not support it. The source-only terminal does not include release, installation or live recovery.
