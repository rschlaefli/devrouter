# Recover canonical profiles and preserve resumable rollback configuration

## Approved scope

User approved two bounded source repairs and owner-run validation: canonicalize
historical combined selections for initial stop; fix first-transition rollback
leaving generated configuration inconsistent with captured degraded state.
Keep immutable journal and exact ownership, worker, resource, config proofs.
No watchdog, deletion/reset, manual journal changes, adoption or live commands.
Existing citation recovery may remain blocked if no trustworthy rollback-write
provenance exists; desired hashes and plausible generated shape cannot prove it.
Source fix + tests + reviews + draft delivery authorized. No new merge/release.

## Baseline and owners

Base ffdeb0925db46f300f0f71ff1f764f1e3f39456f; branch
rs/profile-rollback-recovery at trees/rs/profile-rollback-recovery.
Root docs/project/. Full path for lifecycle ownership risk. PR95 separate.
Main owns architecture/provenance. Explorer completed receipt mapping: no durable
rollback bytes/subset receipt exists, confirmed by consumer owner inspection.

## Delegation Map

- S1: executor; independent source and tests.
- S2: main; independent implementation and stateful sequence verification.
- S3: main; depends on accepted S1/S2 evidence and reviews.

## Feature-wide test portfolio

- Canonical resolution and raw authority: extend existing stop tests.
- Captured-config rollback and next-attempt consistency: extend existing ensure tests.
- Legacy no-capture rollback: none; retain existing regression.
- Unchanged ownership/resource refusals: none; reuse existing suites.
- Documentation: none; native documentation checks.

Each slice consumes this portfolio without adding obligations.

## Slices

S1: src/core/managed-devsy-stop.ts and existing managed-devsy-stop.test.ts.
Resolve the recorded selection through existing resolveProfile on raw config,
compare loaded runtime to canonical name, repeat canonical check on every
observation; preserve raw entry/fence authority equality. No sort-only bypass of
profile existence/validity. Test raw reordered/duplicate/whitespace combined
selection with actual resolver, missing profile and changed selection/config
refusals, raw journal unchanged and stopped containers. No public flags.

S2: src/core/workspace-ensure.ts and existing workspace-ensure.test.ts.
When first transition has persisted capturedStopState but no previous state,
preserve its attempted-profile generated config during rollback. Validate current
generated file and captured source/effective hashes against the attempted plan;
refuse drift, never replace it. Rollback prior services/processes exactly as
before. No captured baseline retains legacy first-transition restoration path.
This prevents creating inconsistent generated config after baseline capture.
Use sequence regression: failing first ensure captures degraded state; rollback
restores prior running service/process set but retains captured config; later
same-profile repair may pass configuration checks with exact live proofs.
Carry persisted captured state and actual generated bytes from the failed first
invocation into the second; do not reset to a valid fixture. Use the real generated
validator with synthetic files or a stateful equivalent checking actual bytes.
Prove retry succeeds when restored resources satisfy existing proofs and remains
refused when restored services/processes fall outside the attempted profile.
Configuration consistency alone does not establish resumability. Test manual
generated drift remains unchanged and refused; no restoration/adoption.
If explorer identifies sufficient existing provenance, derive bounded recovery
extension and return to same planner before coding it; otherwise explicitly
report existing citation as blocked, never infer retroactive proof.

S3: update docs/solutions/runtime-error/pre-registration-stop-remains-stopping.md
and docs/knowledge/managed-environment-lifecycle.md only as applicable. Commit
this plan at docs/project/2026-09-12-profile-rollback-recovery-plan.md;
record actual source/CI evidence, limits and owner-run dogfood path in plan/PR.
No new runtime modules or state schemas planned. One draft PR for both fixes.

## Verification and gates

Red/green regression each behavioral slice. Focused managed-stop, ensure,
repo-config and runtime-state suites, typecheck, Biome/Knip/docs/knowledge,
full tests/build/package and CI. Existing safety tests reused for unchanged
ownership boundaries. No consumer/runtime tests here. Simplifier and slice risk
review on substantive committed slices; final Claude CLI integrated review after
checks. Optional AGY terminal read_file denial from this task is reusable.
Planner challenge now, execute within approved batch once approved. Checkpoint
limitations honestly; source prevention is not proof of legacy recovery.

## Progress

Executable batch approved; planner approved after one correction round requiring
real generated bytes across retries and separate resource-refusal coverage.
S1 and S2 implemented. S1 stop tests: 111 passed; baseline 14 failed with raw
combined selection fixtures. S2 ensure tests: 122 passed; all four new sequence
cases failed before the fix and passed after. S2 simplifier found no useful
simplification; safety review passed. S1 safety review passed. Integrated review found two low-severity test/comment
issues; both corrected with focused tests passing. No blocking lifecycle or
ownership findings remained.

Typecheck, Biome, Knip, documentation checks, build and package smoke passed.
Concurrent full-suite runs each hit one unchanged controller timing timeout;
each affected suite passed in isolation. The full suite passed all 2253 tests with two workers.
Linux process-helper checks skip on macOS; CI34709908921 passed pnpm test
and all required checks on bfda18c, including controller/capacity qualification. No runtime
smoke performed because consumer lifecycle belongs to its owning task.

Existing historical generated drift remains unproved: neither source inspection
nor the owner's scoped artifact/journal search found producing bytes/subset
provenance. Supported canonical stop with a valid retained stop baseline contains
the runtime while preserving the checkout; same-checkout resume is blocked.
No adoption, state rewrite or runtime operation was performed.

Delivery: [draft PR 96](https://github.com/rschlaefli/devrouter/pull/96); source only, no merge/release/install.
Substantive source/test size before resolver simplification: 238 added / 17 removed lines.
Reviewed source correction: 88082c7. The PR tracks final CI; owner dogfood is pending.
Consumer legacy recovery remains blocked independently of source delivery.
S1 simplifier removed an unreachable empty-name branch; focused tests remain green.
