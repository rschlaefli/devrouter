# workspace ensure supplies the linked worktree's host Git common directory.
services:
  app:
    volumes:
      - type: bind
        source: ${DEVROUTER_GIT_COMMON_DIR}
        target: ${DEVROUTER_GIT_COMMON_DIR}
